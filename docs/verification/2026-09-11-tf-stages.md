# 2026-09-11 分阶段 TF 验证

代码部署位置：`/home/nrc/catkin_ws`。本轮没有推送GitHub。

## 实机结果

2026-09-11在连接真实MID360和飞控、飞控保持未解锁的条件下，依次执行基础、建图、地图保存、重定位。全部17项检查通过。

- 基础层公共TF没有定位边；MAVROS辅助转换保留在内部话题。
- 建图：`odom → camera_init → body → base_link`，没有map。
- 重定位：`map → odom → camera_init → body → base_link`。
- 每条边只有一个发布者；重定位中的局部边由`/ducted/local_lio/fastlio`发布，全局校正由`/world_tf_owner`发布。
- 建图TF与机体里程计比较100组，最大位置误差约1.45e-16 m。
- 重定位在实际校正发布时间戳比较77组，最大位置误差约1.99e-16 m。插值时刻不要求与另一独立估计器的原始样本严格相等。
- 当前场景地图保存成功；没有飞行目标输入话题的发布者。MAVROS自身的target反馈话题与输入命令分开检查。
- 测试创建的ROS进程已停止，没有解锁、起飞或模式切换。

实机记录：`logs/tf-stage-refactor/live-20260911-095346/result.json`。测试程序：`src/ducted_bringup/test/integration/tf_stages_hardware.py`。该脚本显式使用真实硬件，仅在11311端口未被占用时运行，不属于默认自动单元测试。

## 回归与同步

远程编译成功，`ducted_bringup`的114项回归测试零错误、零失败。81个变更源码/文档文件的SHA-256核对一致。768种模块启动组合解析通过，非法模式选择被拒绝；该检查不启动任何节点。

证据：`logs/tf-stage-refactor/verify-build.log`、`tests-final.log`、`source-sync.json`。

## 与原实现的区别

移除公共`odom → map → livox_frame → base_link`链。`body`沿用原MID360内部IMU参考点，安装外参不变。重定位增加独立局部FAST-LIO与全局校正拥有者，代价是额外计算负载；全局配准不再直接发布机体TF。实际运动和飞行性能未在这次静止测试中验证。
