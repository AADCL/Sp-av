# PX4 相对高度任务与原生降落验证

日期：2026-09-12。运行工作空间：`/home/nrc/catkin_ws`。

本次三米任务以准备时的 PX4 局部高度为起飞参考，上升 1 m、沿初始机头方向前飞 3 m、悬停 5 s，然后请求原生 AUTO.LAND。上位机等待原生落地及锁定反馈，不再使用高度五秒不变或自定义低 Z 目标完成降落。新模式不依赖有效 h_agl，保留实时障碍、RC、输入时效、定位一致性和明确启动检查。

| 检查 | 结果 | 证据 |
|---|---|---|
| 远程编译 | 成功 | `logs/native_land_build.log` |
| 飞控单元测试 | 100 项，零错误、零失败 | `build/test_results/ducted_control` |
| 任务单元测试 | 71 项，零错误、零失败 | `build/test_results/ducted_mission` |
| 任务准备测试 | 本地 7 项通过 | `src/ducted_bringup/test/test_forward_profile.py` |
| 完整隔离 ROS 任务 | 24 项通过 | `logs/px4_relative_native_land/result.json` |
| 部署一致性 | 本次 35 个代码/手册文件与部署清单哈希一致 | `logs/px4_relative_deployed_hashes.json` |

隔离测试使用独立 master 11327、实际飞控/任务/EGO节点、合成点云和模拟飞控。场景初始局部中心 Z 为 0.4 m，目标 Z 为 1.4 m，机头偏航 60°，前飞目标约 X=1.5 m、Y=2.598 m。没有提供有效地形高度。任务发出的模式请求依次为 OFFBOARD、AUTO.LAND，各一次；AUTO.LAND 在末点悬停高度接管，未发出自定义下降目标。

模拟飞控接地后延迟两秒锁定，并在接地后对局部 Z 引入 0.2 m 偏移，确认任务以落地和锁定反馈结束，不额外要求高度回到起飞值。测试还覆盖默认输出关闭、未解锁拒绝、起飞通道占用、自定义缓降仍闭锁、单次参考不可重用和任务不会自行重启。所有测试子进程均已退出。

同日只读获得的真实飞控信息：报告 PX4 1.12.3，MPC_LAND_SPEED 约 0.7 m/s，COM_DISARM_LAND 为 2 s。本次没有修改这些参数，没有解锁真实飞控，也没有执行实机飞行。模拟只验证 ROS 协作流程，不复现真实动力学、触地冲击或完整 PX4 内部落地检测。

旧自定义 slow_land 的兼容性限制仍保留，仅作用于自定义下降入口。原生 AUTO.LAND 不依赖该下降算法；旧近地缓降测试结果作为历史记录保留，不应据此认为当前自定义缓降已经开放。
