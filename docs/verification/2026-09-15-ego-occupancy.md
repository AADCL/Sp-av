# EGO 起点占据检查修正（2026-09-15）

这是当天早期修正的历史记录。17:38 飞行日志仍出现局部终点误判，后续改为“体素初筛＋实际点距复核”；当前实现及验证以 [后续终点检查记录](2026-09-15-ego-endpoint.md) 为准。

用户实飞日志显示 OFFBOARD、解锁、相对起飞点上升成功；提交 EGO 目标后返回 `current body intersects local occupancy`，恢复后再次暂停。第二次任务随后正常切入 AUTO.LAND，日志记录落地并锁定。本轮助手未执行任何实机控制命令。

## 证据与边界

远程只读核对确认使用 `/home/nrc/catkin_ws/maps/flight_test_01/GlobalMap.pcd` 重定位；导航订阅的是实时 `/ducted/localization/cloud_registered`，经精确时刻 TF 转换到 odom。EGO 起点检查覆盖机体四周的三维包络，而非仅向前检查。

本次飞行没有保存当时的点云 rosbag。飞行结束后采集了 8 帧点云和 22 帧外部里程计，保存于 `logs/ego_occupancy_audit_20260915/`。将这批后采点云代入历史恢复位置，只用于复算量化行为，不能视为当时现场的完整回放。以历史位置 (-0.060387, 0.024692, 0.913447)、日志速度约 0.053863 m/s 估算，安全半径约 0.6865 m；后采点云最近点距离约 0.79～0.80 m。8 帧旧栅格均判为起点占据，修正后这组复算均不占据。更高速度的另一个历史位置仍有保守占据，未通过削减制动或机体余量强行放行。

独立 C++ 复现使用起点 (0,0,0)、障碍 (0,-0.79,0.20)、安全半径 0.6864952 m：旧实现返回 occupied=1，新实现返回 occupied=0。该障碍距起点约 0.815 m，位于所需安全半径之外。

## 修改

- 旧栅格以 `clearance + sqrt(3)*0.1 m` 形成各向同性膨胀模板。新模板改为两个闭合体素盒之间的最短距离，并额外保留半个轨迹采样步长（0.0125 m）。源点去重仍占据整个源体素，查询体素中所有可能碰撞的位置仍被覆盖。
- 保留机体外接球、净空、制动、定位和运动余量；没有删除低处点云、放行起点碰撞或改为直飞最终目标。
- 起点占据告警增加触发点数量、中心与最近触发点 odom 坐标、距离、安全半径和速度。
- 新话题 `/ducted/ego/blocked_obstacles` 保留最近一次起点阻塞快照，后续起点检查通过时清空；暂停期间不持续更新。已纳入录包入口和操作说明。
- 用户最终要求继续保留 0.60×0.70×0.20 m 中心机体尺寸；共享几何文件没有修改。外参、基础系统、建图、重定位入口没有修改。

## 验证

- 远程 ARM64 `catkin_make -j3 -l3` 成功。
- C++ 3 项测试通过：量化误判、跨体素角落的碰撞覆盖、完整线段和高度／范围约束。
- 独立主站 11325 的 EGO 20 项检查通过，包含正常曲线、绕障、密闭通道拒绝、终点检查、起点碰撞诊断以及修正后的侧方点案例。测试结束后该端口已关闭。
- 未重启实机控制节点，未切模式、解锁、起飞或降落；未上传 GitHub。运行中的旧进程需在用户下次重启原联动入口后使用新代码。修复后真实飞行效果尚未验证。

远程记录：`logs/ego_occupancy_audit_20260915/{build.log,unit.log,core.log,source_before.tar.gz,collected.tar.gz}`。
