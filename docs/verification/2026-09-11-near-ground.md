# 近地起降软件验证记录

日期：2026-09-11。工作空间：`/home/nrc/catkin_ws`。

接地准备支持显式地面参考，垂直起飞后必须取得真实测高才能开始航点；缓降只允许在限定近地区域使用本次落地区的短时参考。雷达测量无效时仍输出 `valid: false`，并发布原因。

| 检查 | 结果 | 远程记录 |
|---|---|---|
| 工作空间编译 | 成功 | `logs/near_ground_build.log` |
| 针对性单元测试 | 129 项通过：接地参考 8、飞控 71、自动序列 17、任务准备 6、高度运行逻辑 27 | `logs/near_ground_unit.log` |
| 完整任务，起飞及接地前存在测高盲区 | 23 项通过 | `logs/ground_contact_isolated/result.json` |
| 起飞后始终没有有效雷达高度 | 16 项通过，中止并保持位置，无航点目标 | `logs/ground_contact_no_acquisition/result.json` |
| 默认启动配置解析 | 输出默认关闭，不加载接地参考 | `logs/near_ground_launch_params.yaml` |

完整模拟场景的地面绝对 Z 为 0.30 m，初始中心 Z 为 0.40 m，起飞目标 Z 为 1.30 m。机头偏航 60°，前飞目标约为 X=1.50 m、Y=2.598 m；实际到达后悬停并缓降。测得下降位置目标最大变化率约 0.2016 m/s，接地反馈后才请求 AUTO.LAND。

测试运行在独立 ROS master 11327，使用模拟飞控、合成点云和高度失效输入；没有执行实机飞行。两个场景的测试子进程均已退出。

本记录验证起降状态机及失效处理，不代表实机 MID360 已能在所有高度识别地面。若实际起飞后持续无法获得有效测高，程序会中止后续前飞。
