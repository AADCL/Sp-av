# 地图导出速度与密度修正

验证主机：`nrc@192.168.50.140`，工作空间 `/home/nrc/catkin_ws`，2026-09-12。

使用 `logs/mapping-scans-1IYMLp` 的前2008帧，以及 `maps/site_test_02/transformations.pcd` 中相同的37个优化关键帧位姿回放。未启动实机飞行或修改飞控参数。

| 对比项 | 原流程 | 新流程 |
|---|---:|---:|
| 回放及导出耗时 | 84.17 s | 29.92 s |
| 默认静态地图体素 | 内部10 cm，客户端再降至20 cm | 5 cm，客户端不再额外降采样 |
| 最终静态点数 | 9,851 | 229,549 |
| 额外观测占据栅格 | 始终生成 | 默认关闭，按需启用 |
| 半径去噪准备 | 串行 | 两个有界并行任务 |

原现场保存日志记载94.1 s；表内84.17 s来自本次同一主机的离线旧算法计时，其中占据栅格33.32 s、半径去噪31.28 s、时间过滤16.38 s、读文件2.23 s。新流程调用实际部署的 `ScanArchive::replay`，包含三份静态PCD和元数据写入。处理时间随帧数、场景密度和主机负载变化，不是每次保存固定30秒。

动态过滤仍采用原来的8帧、2秒静态确认条件，按原扫描顺序处理。另将新版暂设回10 cm原生体素作对照，得到相同48,966个静态体素；坐标最大差为4.77×10⁻⁷ m，处于浮点舍入量级。单位测试还检查开启/关闭额外占据导出及单/双准备任务时，静态PCD字节一致。

较密的完整地图已另存到 `/home/nrc/catkin_ws/maps/site_test_02_dense/GlobalMap.pcd`，原 `site_test_02` 保留。该地图从原扫描与原优化位姿重新导出，尚未用它重新执行实机重定位。

针对性验证结果：

- 完整 `catkin_make -j2 -l3` 成功。
- 地图C++四项测试通过，包括细节保留、可选占据、并行一致性、档案缺失及动态过滤回归。
- 保存客户端14项、建图配置契约8项通过。
- v2元数据的六文件地图包通过实际客户端完整性校验；v1七文件格式兼容保留。

证据保存在远程 `logs/map-density-benchmark/`：`baseline.json`、`optimized.json`、`compatibility.json`、`comparison.json`、`build-result.json`、`tests.json` 及对应日志。导出密度的修改不改变FAST-LIO在线0.5 m匹配降采样、实时障碍点云和机体外参。
