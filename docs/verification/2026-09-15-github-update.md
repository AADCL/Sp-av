# 2026-09-15 工作空间发布核对

目标仓库：`AADCL/Sp-av`，主分支 `main`。代码来源为远程 `/home/nrc/catkin_ws`，本地发布副本与远程源码进行 SHA-256 核对。

## 本次发布内容

- 发布现有 C++ `ducted_offboard`：任务状态机、飞控执行器、独立服务线程和异步日志；保留分步 0/1/2/3 指令。
- EGO 源码及离线测试保留，生产运行入口冻结；直接跟踪没有自动避障。旧 Python 自动控制与任务执行模块从活动源码中移除，变更前备份保存在远程日志目录。
- 同步此前已部署的 RC 数据时效、FAST-LIO 盲区和相关接口修复。
- 更新仓库/工作空间/控制包 README、四份中文手册及地图说明。操作顺序为基础系统、定位、控制器、加载任务、起飞、执行、降落。
- 地图由用户选择，`flight_test_01/GlobalMap.pcd` 只是手册的默认示例；通过 `MAP_FILE` 传给 `map_file` 可替换地图。已有地图不是控制器必需项，可改用建图定位。
- 补充远程 `flight_test_01` 的六个地图与元数据文件。构建产物、原始日志、缓存、备份归档和本地凭据不进入仓库。

## 发布前复查

| 检查 | 结果 |
|---|---|
| `test_split_runtime` | 14 个实际 C++ testcase，0 失败、0 错误 |
| `test_offboard_core` | 17 个保留核心 testcase，0 失败、0 错误 |
| `test_ego_core` | 10 个冻结桥接纯算法 testcase，0 失败、0 错误 |
| `test_mission_loader` | 4 个任务加载 testcase，0 失败、0 错误 |
| `frozen_launch_checks.py` | 2 组通过：EGO 入口不启动控制/规划，基础/建图/重定位入口保留 |

以上命令在远程已有构建上重新执行，日志保存于 `logs/github_update_20260915/`；此次发布未修改控制算法。完整编译、隔离 ROS 任务循环、线程停顿和故障测试沿用[分层版本验证记录](2026-09-15-offboard-split.md)。

用户已反馈完成测试，但没有提供本次测试的量化记录；本文件不据此增加飞行性能结论。发布操作没有向真实飞控发送切模式、解锁或位置目标指令。
